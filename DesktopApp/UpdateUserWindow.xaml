<Window x:Class="DesktopApp.UpdateUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopApp"
        mc:Ignorable="d"
        Title="Aktualizovat uživatele" Height="225" Width="300" ResizeMode="NoResize">
    <Grid>
        <StackPanel>
            <Label Content="Detail rezervace" HorizontalAlignment="Center" Margin="0,0,0,10" FontSize="20"/>
            <DockPanel>
                <Label Content="Jméno: "/>
                <TextBox x:Name="name" Text="{Binding Name}"/>
            </DockPanel>
            <DockPanel>
                <Label Content="Příjmení: "/>
                <TextBox x:Name="surname" Text="{Binding Surname}"/>
            </DockPanel>
            <DockPanel>
                <Label Content="Email: "/>
                <TextBox x:Name="email" Text="{Binding Email}"/>
            </DockPanel>
            <DockPanel>
                <Label Content="Heslo: "/>
                <TextBox x:Name="heslo" Text="{Binding Password}"/>
            </DockPanel>
            <Button x:Name="UpdateUserButton" Click="UpdateUserButton_Click" Content="Uložit" HorizontalAlignment="Center" Width="150" Margin="0,0,0,10"/>
        </StackPanel>
    </Grid>
</Window>
